//variables

//answer word

//VARIABLES
var myArray = ["fire", "crackle", "hiking", "mountain", "desert"];
var rand = myArray[Math.floor(Math.random() * myArray.length)];
// console.log (rand);
gameWord = function () {
    myArray[Math.floor(Math.random() * myArray.length)];
}

var gameWord;

guess = event.key
for i = 0 ....is = word.lenth

//split random word into an array and check each letter to the array
//rock paper scissors for storing onkeyup
function splitString(rand, separator) {
    var arrayOfStrings = rand.split("");


var userGuess = document.onkeyup
//container holding user input from onKeyUp

//establish answerWord from a bank of strings, sring used to compare letters

//listen for input from user onKeyUp, store as var userGuess
document.getElementById("").onkeyup = function() {myFunction()};

document.onkeyup = function(event) {
    userText.textContent = event.key;
  };
//has letter been guessed? 

//if yes, do nothing

//if no, reduce guesses-left by 1, compare userGuess to letters in the string 

//if letter is part of string, add letter in correct spot to word-blanks

//if not, then add letter to wrong-Guesses
 
//listen for input from user onKeyUp

